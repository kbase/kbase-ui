## Kbase UI Deployment Profile
---

# This is the key used to select the deployment config to be merged into the
# service config template, so it essentially selects the service runtime.
#
# possible values: ci, next, prod
#
# see: /deploy-ci.cfg, deploy-next.cfg, deploy-prod.cfg, /config/service-config-template.yml
serviceTargetKey: prod
uiTargetKey: prod

