---
description: Accessing Relation Engine Landing Pages with authentication
cases:
  - description: should load the re-landing-pages plugin
    tasks:
      - subtask: login
      - action: navigate
        path: review
      - subtask: plugin
      - wait: forElement
        selector:
          - type: plugin
            value: re-landing-pages
