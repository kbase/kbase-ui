# "Logs out" the browser by removing the KBase session cookie.
---
title: Logout Task
tasks:
  - action: deleteSessionCookie
    title: Delete kbase session cookie
    params:
      name: kbase_session
      path: "/"
