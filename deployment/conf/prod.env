# KBase UI CI Config

deploy_environment=prod
deploy_hostname=kbase.us
deploy_icon=thumbs-up
deploy_name=Production

ui_backupCookie_domain=kbase.us
ui_backupCookie_enabled=true

ui_services_analytics_google_hostname=kbase.us
ui_services_analytics_google_code=UA-74532966-1
allow=[]

services_narrative_url=https://narrative.kbase.us

# services 

# list of plugins to remove from the runtime even if specified in the build
# comma separated list
# ui_services_plugin_disabled=["data-search", "reske-simple-search"]

# list of sidebar menu items to remove
ui_services_menu_hamburger_disabled=["jgi-search", "simple-search", "reske-admin", "staging-browser"]

ui_services_menu_sidebar_disabled=["feeds"]
